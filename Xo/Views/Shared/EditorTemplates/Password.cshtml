@{
    var htmlAttributes = new RouteValueDictionary(new { @class = "password-editor form-control" });
    if (ViewData.ModelMetadata.IsRequired)
    {
        htmlAttributes.Add("aria-required", true);
    }
}
@Html.Password("", ViewData.TemplateInfo.FormattedModelValue, htmlAttributes)